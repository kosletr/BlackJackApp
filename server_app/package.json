{
  "name": "blackjack_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "export NODE_ENV=testing || set NODE_ENV=testing&& jest --watchAll --verbose --coverage",
    "test:unit": "export NODE_ENV=testing || set NODE_ENV=testing&& jest --watchAll --verbose tests/unit",
    "test:int": "export NODE_ENV=testing || set NODE_ENV=testing&& jest --watchAll --verbose tests/int"
  },
  "engines": {
    "node": "16.17.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.7",
    "winston": "^3.8.1",
    "ws": "^8.8.1"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^2.0.19"
  }
}